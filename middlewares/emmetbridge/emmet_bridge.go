package emmetbridge

// This package is imported for side effects by middlewares/autoload.
// The implementation is currently optional; keeping the package allows builds
// to succeed even when the middleware is not enabled.
